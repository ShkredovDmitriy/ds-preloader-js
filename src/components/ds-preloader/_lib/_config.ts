const config = {
  prl: ".ds-prl",
  prlOpen: "ds-prl-open",
  prlClose: "ds-prl-close",
  load: ".ds-prl-load",
  body: "body",
  bodyNoShow: "ds-prl-body",
  bodyNoScroll: "ds-prl-no-scroll"
};

export default config;
